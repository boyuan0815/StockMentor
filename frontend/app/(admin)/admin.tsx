import { PlaceholderScreen } from '@/components/foundation/placeholder-screen';

export default function AdminDashboardRoute() {
  return (
    <PlaceholderScreen
      eyebrow="Admin placeholder"
      title="Admin dashboard"
      description="Phase 6 will add the web and tablet admin console. Admin API calls will require Basic Auth plus X-Admin-Token."
    />
  );
}
